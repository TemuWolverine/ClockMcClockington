packages:
  core: !include ../clock.yaml

esphome:
  project:
    name: temuwolverine.clockmcclockington
    version: dev

dashboard_import:
  package_import_url: github://TemuWolverine/ClockMcClockington/clock.yaml@main
  import_full_config: true

improv_serial:

http_request:

ota:
  - platform: http_request

update:
  - platform: http_request
    name: Firmware
    source: https://temuwolverine.github.io/ClockMcClockington/firmware/clockmcclockington.manifest.json
